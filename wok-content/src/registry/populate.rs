//! `populate_from_scene` lands in step 5 (per plan section 11). Step 2 leaves this module as
//! a stub so the `Registry` module tree compiles ahead of the populate implementation.

// Intentionally empty: contents land in step 5.
