//! `transition_chunk` - flip a slot's runtime eagerness. Phase A step 7 fills in this
//! module; step 6 leaves a stub so the chunk module tree compiles.

// Intentionally empty: contents land in step 7.
